extends MarginContainer


# Declare member variables here. Examples:
# var a = 2
# var b = "text"
onready var fore_aft = $VBoxContainer/Fore_Aft
onready var up_down = $VBoxContainer/Up_Down
onready var left_right = $VBoxContainer/Left_Right

# Called when the node enters the scene tree for the first time.
func _ready():
	var forward = $"../..".forward
	var up = $"../..".up
	var left = $"../..".left

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass


func _on_Player_direction_change(forward, up, left):
	$VBoxContainer/Fore_Aft.text = str(forward)
	$VBoxContainer/Up_Down.text = str(up)
	$VBoxContainer/Left_Right.text = str(left)
